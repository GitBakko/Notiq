# @emoji-mart/react declares peer react ^16.8||^17||^18 but the app runs React 19.
# The dependency works fine with React 19; legacy-peer-deps keeps `npm ci` (CI) and
# local installs consistent. Tracked for cleanup (consider dropping the duplicate
# emoji lib / upgrading @emoji-mart/react).
legacy-peer-deps=true
