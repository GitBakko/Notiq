# HANDOFF — hardening kanban + offline-first

**Scritto il 2026-09-01.** Leggi questo per primo. È autosufficiente: non serve la cronologia della sessione precedente.

---

## 1. Dove sei

Branch **`fix/kanban-hardening`**, 64 commit, **mai pushato, nessuna PR**. Parte da `main` a `9270d61`.

Stato verificato al momento della scrittura:

| | |
|---|---|
| Frontend unit | **19 file / 211 test** verdi |
| Backend unit | **63 file / 1150 test** verdi |
| Typecheck | entrambi puliti |
| e2e | **12 passati** su 4 spec kanban/offline (`kanban`, `kanban-account-scope`, `kanban-offline-sync`, `offline-first`) |

Comandi (non indovinarli, sono stati sbagliati più volte):

```bash
cd backend  && npx vitest run && npx tsc --noEmit
cd frontend && npx vitest run && npx tsc -p tsconfig.app.json --noEmit
cd frontend && npx playwright test e2e/<spec>      # avvia Vite da solo
```

> ⚠️ **`cd frontend && npx tsc --noEmit` esce 0 senza compilare nulla** — `tsconfig.json` ha `"files": []`. Non è una verifica valida. Usa sempre `-p tsconfig.app.json`.

**Stack di sviluppo:** DB in Docker (`docker start notiq-db`, porta 5433), backend `cd backend && npm run dev` su :3001. Serve per gli e2e. `curl localhost:3001/health` deve dire `db:up`.

---

## 2. I due piani

### `docs/superpowers/plans/2026-08-31-kanban-hardening.md` — **26/46**

Nasce da un audit di 185 finding verificati (la spec è `docs/plans/2026-08-31-kanban-hardening.md`, con tutti i finding in appendice). Chiusi: Stage 0 (superficie d'errore), Stage 1 (10 buchi di autorizzazione, due critical), Stage 2 (riscrittura dell'ordinamento), Stage 3 a 4/7.

### `docs/superpowers/plans/2026-09-01-offline-first-mutations.md` — **8/10**

Nasce da un bug trovato per caso: il `networkMode` di default di TanStack Query metteva in pausa **ogni** mutation offline, quindi le scritture local-first non arrivavano mai a Dexie. Gli 8 task originali sono chiusi; i task **9 e 10** sono stati aggiunti a fine sessione e non sono mai partiti.

Entrambi i piani hanno un **ledger SDD** con ogni decisione presa e ogni minor rinviato:
`.superpowers/sdd/2026-08-31-kanban-hardening/progress.md` e `.superpowers/sdd/2026-09-01-offline-first-mutations/progress.md`.
Sono **gitignored** — esistono solo su questa macchina. Se ti servono altrove, vanno committati.

---

## 3. ⚠️ Tre trappole, leggile prima di toccare qualsiasi cosa

**1. I task 3.4, 3.6 e 3.7 citano codice che non esiste più.** Il piano offline e il task 6 hanno riscritto proprio le righe che quei task riportano come stato "prima". In particolare:

- **3.6 Step 4 prescrive un `where('syncStatus').equals('synced').toArray()` non scopato.** Quel filtro oggi porta uno scope per utente (`b.ownerId === currentUserId`), aggiunto perché il pull dell'account A faceva `bulkDelete` delle board valide dell'account B. **Applicarlo alla lettera cancellerebbe una guardia viva contro la perdita di dati cross-account.** La forma corretta è `(b.ownerId === currentUserId || b.viewerId === currentUserId)`.
- **3.6 Step 5 cancellerebbe un contratto atterrato:** il blocco che elimina contiene ora `prunedBoardIds.push(...staleIds)`, consumato da `syncInvalidation.ts` e pinnato dai test.
- **3.4 assume `syncPush(): Promise<void>`.** È `Promise<boolean>` dal task 5 del piano offline, e un test pinna esattamente il comportamento che 3.4 doveva rimuovere.

**Rileggi quei tre contro il codice attuale prima di eseguirli.**

**2. `.superpowers/` è gitignored, ma nove commenti nel codice lo citano.** `card.service.ts` e i suoi test rimandano a `task-2.7-report.md` e `task-2.8-report.md` per i residui documentati del locking in `moveCard`. Su un clone fresco quei riferimenti sono vuoti — ed è proprio ciò che serve leggere prima di toccare `moveCard`. O inlinei le due frasi che contano, o committi quei report.

**3. Il default della libreria decide al posto tuo.** Se aggiungi una mutation la cui `mutationFn` scrive su Dexie e non le spreddi `LOCAL_FIRST` (`frontend/src/lib/networkMode.ts`), offline **non gira**, in silenzio, e nessun test lo intercetta a meno che non spenga davvero la rete. La convenzione è in `CLAUDE.md` sotto `### Convenzioni`.

---

## 4. Cosa fare, in ordine

### Primo, indipendentemente dal resto

**Task 6.5 del piano kanban — collegare Playwright alla CI.** 20 spec nel repo, **zero riferimenti in `.github/workflows/ci.yml`**, nessuno script `e2e` in `package.json`. La CI esegue solo gli unit test.

Il motivo per cui è il primo: il bug più grave di questa sessione è sopravvissuto a un audit da 38 agent e **è stato trovato solo costruendo un e2e offline vero**. Tutti i test "offline" del repo intercettavano le rotte con `page.route().abort()`, che lascia `navigator.onLine` a `true` — quindi la pausa non scattava mai e l'illusione reggeva. La guardia che impedirebbe il ritorno di quel bug (`offline-first.spec.ts`) oggi gira solo se qualcuno se ne ricorda.

### Poi, per valore decrescente

| Priorità | Cosa | Dove |
|---|---|---|
| Alta | **4.3** — un collaboratore a cui revochi lo share continua a ricevere card, commenti e chat finché tiene il tab aperto | kanban Stage 4 |
| Alta | **4.1** — `broadcast()` manda il titolo della nota collegata a chi non può vederla | kanban Stage 4 |
| Alta | **5.1** — `getBoard` **scrive** a ogni lettura, ed è anche la fetch per-board di `syncPull` ogni 30s | kanban Stage 5 |
| Media | **9** — le query in pausa mostrano il ramo vuoto senza dire perché (35 su 39) | offline |
| Media | **5.4** — chat e commenti paginano dai **50 più vecchi**: oltre i 50 la chat è congelata | kanban Stage 5 |
| Media | **3.4, 3.6, 3.7** — ma solo dopo averli riscritti (trappola 1) | kanban Stage 3 |
| Bassa | **10** — `tasklist.service.ts`, `aggregate`+`create` fuori transazione | offline |
| Bassa | Stage 6 kanban — a11y, filtri, cover/avatar, query limitate | kanban Stage 6 |

### Il minor che promuoverei a task

**`unarchiveCard` non riassegna `position`.** È l'unico percorso rimasto che produce la classe di difetto che lo Stage 2 esisteva per eliminare: una card ripristinata torna sulla sua posizione stale e può collidere con una viva. Si auto-ripara al move successivo e non c'è vincolo di unicità, quindi non è urgente — ma è incoerente lasciarlo dopo aver riscritto tutto il resto dell'ordinamento.

---

## 5. Come si è lavorato, se vuoi continuare così

Il metodo che ha prodotto quasi tutto il valore: **un implementer per task, poi un reviewer separato che traccia invece di leggere.** Gli implementer hanno seguito i brief correttamente; i finding veri sono venuti dai reviewer che sono andati a vedere cosa fa davvero il codice a valle.

Quattro abitudini che si sono ripagate, in ordine di resa:

1. **Chiedere la conferma rossa.** Un test che non è mai stato visto fallire è una dichiarazione d'intenti. La suite kanban originale passava *con il bug di `position` dentro* proprio perché nessuno l'aveva verificato.
2. **Far verificare le affermazioni, non accettarle.** Più volte un report ha dichiarato una cosa ("questo ramo è irraggiungibile", "questa è l'unica via", "ho controllato gli altri conteggi") che si è rivelata falsa o incompleta al controllo.
3. **Nominare il caso che preoccupa** invece di lasciarlo trovare. Le review migliori sono arrivate quando ho scritto nel prompt lo scenario esatto che temevo.
4. **Far giudicare le decisioni dal lato opposto.** Ogni volta che un implementer ha scelto fra due opzioni difendibili, chiedere al reviewer di argomentare contro ha trovato qualcosa.

Le convenzioni introdotte da questo branch, da rispettare se continui:

- **Doppia asserzione sugli errori** — classe **e** chiave i18n, perché `app.ts` manda `error.message` letterale al client.
- **Doppio `tx` distinto** nei test di transazione — `setup.ts` passa `mockPrisma` come `tx`, quindi senza un doppio separato l'asserzione non distingue dentro da fuori. *Sei asserzioni su otto nel repo sono ancora deboli così.*
- **`[BACKUP] <data> — <motivo>`** sul codice sostituito, non sulle aggiunte pure.
- **`ponytail:`** per una scorciatoia accettata, con dentro il tetto noto e la via d'uscita.

---

## 6. Le decisioni prese, e cosa costano se sono sbagliate

46 ruling nei due ledger. Le quattro che rivedrei per prime:

- **Piano kanban fermato a 26/46** per affrontare l'offline-first. *Se sbagliato:* venti task restano aperti più a lungo.
- **`LOCAL_FIRST` opt-in invece di default globale.** *Se sbagliato:* una nuova mutation local-first senza la costante riproduce il bug in silenzio. La difesa è una riga in `CLAUDE.md`, niente lint.
- **Niente `archivedAt` su `LocalKanbanCard`** per le card fantasma. *Se sbagliato:* un piazzamento sfasato di N posizioni in una finestra di 30 secondi, auto-riparante.
- **Niente `@@unique([columnId, position])`.** I dati di produzione hanno già duplicati dal bug vecchio: la migration fallirebbe. Il resequence più il tiebreaker rendono l'ordine deterministico senza toccare il DB.

---

## 7. Da fare quando lo decidi tu

Il branch **non è pushato e non c'è PR**. 64 commit. È una modifica verso l'esterno e nessuno l'ha autorizzata.

---

## 8. Ripartenza in tre righe

```bash
git checkout fix/kanban-hardening
docker start notiq-db && cd backend && npm run dev &     # serve solo per gli e2e
cat docs/superpowers/plans/2026-09-01-HANDOFF.md          # questo file
```

Poi: leggi la sezione 3 (le trappole), scegli dalla sezione 4, e prendi il brief del task dal piano corrispondente. I ledger in `.superpowers/sdd/*/progress.md` hanno il dettaglio di ogni decisione se ti serve il perché.
